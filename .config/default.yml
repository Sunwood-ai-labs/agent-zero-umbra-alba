url: __MISSKEY_URL__/
port: 3000

db:
  host: db
  port: 5432
  db: __POSTGRES_DB__
  user: __POSTGRES_USER__
  pass: __POSTGRES_PASSWORD__

redis:
  host: redis
  port: 6379

fulltextSearch:
  provider: sqlLike

id: aidx
clusterLimit: 1
setupPassword: __MISSKEY_SETUP_PASSWORD__

# This is a local agent sandbox. Keep federation-facing fetches blocked.
allowedPrivateNetworks:
  - 127.0.0.1/32
