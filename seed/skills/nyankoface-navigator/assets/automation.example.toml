schema_version = 1
name = "Weekly repository report"
description = "Summarize repository activity without changing files."
platform = "codex"
format = "automation"
version = "1.0.0"
schedule_type = "weekly"
timezone = "<IANA_TIMEZONE>"
trigger = "<WEEKLY_SCHEDULE>"
required_permissions = ["repository:read"]
required_connectors = ["github"]
workspace_required = false
delivery_type = "none"
tested_on = ["Codex Desktop"]
tags = ["report", "repository", "read-only"]
license = "MIT"
enabled = false
required_secrets = ["GITHUB_TOKEN"]
notes = "Replace placeholders after import and keep the Automation disabled until review is complete."
